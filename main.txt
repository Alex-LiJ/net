/*任意指定路由器号生成其路由表并输出
增删原拓扑中指定的任意边.顶点重新生成路由表*/
//开始界面,选择操作：指定点生成表/增加边、点/删除边、点/退出程序

#include<iostream>
using namespace std;
int main(void) {
	int a=0;                //选择操作的变量
	cout << "--------------------------------------" << endl;
	cout << "         网络拓扑路由表生成系统       " << endl;
	cout << "--------------------------------------" << endl;
	cout << endl;
	cout << endl;
	cout << "请选择操作：" << endl;
	cout << "A:选择路由器生成路由表" << endl;
	cout << "B:增加一个顶点" << endl;
	cout << "C:删除指定顶点" << endl;
	cout << "D:增加一个边" << endl;
	cout << "E:删除指定边" << endl;
	cout << "F:退出系统" << endl;
	while (a != 'F') {
		cin >> a;
		switch (a)
		{
		case 'A':
			createtable();
		case 'B':
			addpoint();
		case 'C':
			delpoint();
		case 'D':
			addedage();
		case 'E':
			deledage();

		}
		a = 0;
		cout << "请选择操作：" << endl;
	}
	return 0;

}